---
phase: 01-running-state-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kitty/state.py
  - kitty/state.conf
  - tests/state-test.py
autonomous: true
requirements: [SEAM-01]
must_haves:
  truths:
    - "Focusing a stail board pane writes that board's name to $STATE/active atomically"
    - "Focus loss clears the file only if it still names the lost board (compare-and-clear never clobbers a newer gain)"
    - "Windows without a valid board user-var never touch the file (charset-gated)"
    - "A watcher exception can never propagate into kitty — the hook is structurally unable to hurt a pane"
  artifacts:
    - path: "kitty/state.py"
      provides: "on_focus_change watcher maintaining $STATE/active; filesystem-only safety contract in header"
      contains: "def on_focus_change"
      min_lines: 30
    - path: "kitty/state.conf"
      provides: "watcher registration (one line, tail.conf analog)"
      contains: "watcher state.py"
    - path: "tests/state-test.py"
      provides: "kitty-stubbed unit suite driving gain/loss/junk-board/exception paths"
      min_lines: 60
  key_links:
    - from: "window.user_vars['board'] (stamped by _emit_session in plan 01-03)"
      to: "$STATE/active"
      via: "on_focus_change gain -> tmp + os.replace; loss -> compare-and-clear unlink"
      pattern: "os\\.replace"
    - from: "tests/state-test.py"
      to: "kitty/state.py"
      via: "repo-relative importlib load (NOT the deployed ~/.config/kitty path)"
      pattern: "dirname\\(__file__\\)"
---

<objective>
Create the focus watcher that gives `stail active` its kdotool-free data source: a new kitty global watcher `kitty/state.py` writes the focused board's name to `$STATE/active` on focus gain and compare-and-clears it on loss. Implements the `active` half of SEAM-01 per open-question resolution OQ-1 (active DOES move off kdotool this phase). A deliberately separate file from tail.py so each watcher keeps a one-screen-auditable safety contract: tail.py = send-text only, state.py = filesystem only.

Purpose: event-driven focus state owned by stail, replacing `kdotool getactivewindow` (consumed by plan 01-04's cmd_active rewrite; deployed by plan 01-05).
Output: kitty/state.py + kitty/state.conf + tests/state-test.py (unit suite that runs without kitty).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-running-state-seam/01-RESEARCH.md
@.planning/phases/01-running-state-seam/01-PATTERNS.md
@kitty/tail.py
@kitty/hold.py
@kitty/tail.conf
</context>

## Artifacts this phase produces

This plan produces: `kitty/state.py` (symbols `on_focus_change`, `_active_path`, `_BOARD_RE`), `kitty/state.conf` (`watcher state.py` directive), `tests/state-test.py`, and the state path `$STATE/active`. See 01-01-PLAN.md for the full phase-wide symbol registry (includes `_run_mark`, `_pane_alive`, `_lab_in_exchange`, `$STATE/run/<lab>/<pid>`, `--var board=`, `STAIL_BIN`, deploy symlinks).

<tasks>

<task type="auto">
  <name>Task 1: kitty/state.py focus watcher + state.conf</name>
  <files>kitty/state.py, kitty/state.conf</files>
  <read_first>
    - kitty/tail.py (header safety-contract style lines 1-29; imports lines 31-34; blanket try/except pattern lines 90-92)
    - kitty/hold.py (user-var read + charset re-validation lines 19-20 and 30-33; XDG_STATE_HOME path construction lines 35-37)
    - kitty/tail.conf (the one-line `watcher` directive analog)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md ("Focus watcher (new kitty/state.py)" verified draft in Code Examples; Pattern 3; Assumptions A3/A4)
  </read_first>
  <action>
    Create `kitty/state.py` implementing the verified RESEARCH.md draft (API `on_focus_change(boss: Boss, window: Window, data: dict)` with `data={'focused': bool}`, verified against installed kitty 0.47.1):
    - Module constants/helpers: `_BOARD_RE = re.compile(r'[A-Za-z0-9._-]+')` (the same charset the CLI enforces); `_active_path()` building `os.environ.get('XDG_STATE_HOME', ~/.local/state)` + `/switchtail`, `os.makedirs(..., exist_ok=True)`, returning `<state>/active`.
    - Hook body, entirely wrapped in `try: ... except Exception: pass` (a watcher exception must never hurt the pane): read `(window.user_vars or {}).get('board')`; return immediately unless the value exists and passes `_BOARD_RE.fullmatch` (non-stail windows and junk values never touch the file). On gain (`data.get('focused')` truthy): write the board name to `<path>.<pid>.tmp` then `os.replace(tmp, path)` — atomic, the Python mirror of stail's `mv -f` primitive. On loss: open the file, and `os.unlink` ONLY if its stripped content equals own board (compare-and-clear, so a gain event from the newly focused board that lands first is not clobbered); `FileNotFoundError` passes silently.
    - File header: copy tail.py's documented-safety-contract style and declare the parallel property — this watcher performs filesystem writes to `$STATE/active` ONLY; it contains no boss/window mutation calls (no send_text, no close/destroy verbs), so its safety audit fits one screen. Note WHY it is a separate file from tail.py (tail.py's audited send-text-only property stays clean). Imports mirror tail.py: `from typing import Any`, `from kitty.boss import Boss`, `from kitty.window import Window`.
    Create `kitty/state.conf` containing exactly one directive: `watcher state.py` (tail.conf analog). Deployment (symlink + kitty.conf include) is plan 01-05's job — do not touch the live config here.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('kitty/state.py').read())" && [ "$(grep -v '^\s*#' kitty/state.py | grep -cE 'send_text|close_window|destroy|call_remote_control')" -eq 0 ] && grep -q 'watcher state.py' kitty/state.conf</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('kitty/state.py').read())"` exits 0.
    - `grep -c 'def on_focus_change' kitty/state.py` = 1; `grep -c 'os.replace' kitty/state.py` >= 1; `grep -c 'except Exception' kitty/state.py` >= 1; `grep -c 'fullmatch' kitty/state.py` >= 1.
    - `grep -v '^\s*#' kitty/state.py | grep -cE 'send_text|close_window|destroy|call_remote_control'` = 0 (filesystem-only contract holds in code, not just in the header).
    - `kitty/state.conf` content is the single directive `watcher state.py`.
  </acceptance_criteria>
  <done>state.py exists with the verified hook implementation, charset gate, atomic write, compare-and-clear, blanket exception guard, and a tail.py-style safety-contract header; state.conf registers it.</done>
</task>

<task type="auto">
  <name>Task 2: tests/state-test.py — kitty-stubbed unit suite for the watcher</name>
  <files>tests/state-test.py</files>
  <read_first>
    - tests/tail-test.py (the kitty-module stubbing harness: sys.modules stubs for kitty/kitty.boss/kitty.window lines 16-36, importlib loader lines 39-46, check()/summary house style)
    - kitty/state.py (as written in Task 1)
  </read_first>
  <action>
    Create `tests/state-test.py` mirroring tail-test.py's harness: stub `kitty`, `kitty.boss`, `kitty.window` in `sys.modules` (state.py needs no fast_data_types stub — it schedules no timers), then load the watcher via importlib. CRITICAL deviation from tail-test.py: load state.py from the REPO-RELATIVE path `os.path.join(os.path.dirname(__file__), '..', 'kitty', 'state.py')`, NOT from `~/.config/kitty/` — this suite must test the edited tree (the same Pitfall-1 honesty STAIL_BIN gives the bash suites; tail-test.py's deployed-path load is the trap, do not copy it). Use a fake Window object exposing `user_vars` (dict) and drive `on_focus_change` directly, pointing `XDG_STATE_HOME` at a fresh tempdir per case. Cases (house ok/✗ style, summary line, non-zero exit on any failure):
    1. Gain with `user_vars={'board':'zlab'}` → `<tmp>/switchtail/active` exists with content `zlab`; no `.tmp` leftover.
    2. Loss with the same board → file removed.
    3. Loss while the file contains a DIFFERENT board (simulating the cross-process gain-first ordering) → file untouched (compare-and-clear).
    4. Gain with no `board` user-var → file never created.
    5. Gain with hostile board values (`'../evil'`, `'a b'`) → file never created (charset gate).
    6. Exception path: `XDG_STATE_HOME` pointed at an unwritable location (e.g. a read-only dir) → `on_focus_change` returns without raising.
    Wiring this suite into run-all.sh is plan 01-03's job (run-all.sh is owned by plan 01-01 in this wave) — do not edit run-all.sh here.
  </action>
  <verify>
    <automated>python3 tests/state-test.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/state-test.py` exits 0 and reports >= 6 passed, 0 failed.
    - `grep -c 'dirname(__file__)' tests/state-test.py` >= 1 and `grep -c '.config/kitty' tests/state-test.py` = 0 (repo-relative load, never the deployed path).
    - Removing the charset gate or the compare-and-clear from state.py would make at least one case fail (cases 3 and 5 assert those behaviors directly).
  </acceptance_criteria>
  <done>state-test.py proves gain/loss/compare-and-clear/junk-rejection/exception-safety against the repo's state.py without a kitty runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kitty user-vars -> watcher | board identity arrives via OSC-settable user variables on the window |
| watcher -> $STATE/active | file consumed by cmd_active and emitted into widget-bound JSON (plan 01-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-W1 | Tampering | hostile/garbled `board` user-var reaching a filesystem path or the active file | medium | mitigate | `_BOARD_RE.fullmatch` gate before ANY use (Task 1); value is written as content only, never interpolated into a path; cmd_active re-gates on read (plan 01-04, defense in depth) |
| T-01-W2 | Elevation | watcher gaining pane-destructive capability over time | high | mitigate | filesystem-only contract: separate file from tail.py, header declares it, and the automated grep gate (no send_text/close/destroy/remote-control verbs) enforces it in CI (Task 1 acceptance) |
| T-01-W3 | DoS | watcher exception killing/destabilizing the kitty process hosting agent panes | high | mitigate | blanket `try/except Exception: pass` around the entire hook body; exception path exercised by state-test case 6 |
| T-01-W4 | Tampering | symlink planted at $STATE/active redirecting the write | low | accept | `os.replace` renames over the symlink itself (does not follow it); state dir is user-owned 0700-class territory on a single-user box |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | zero package-manager installs this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('kitty/state.py').read())"` exits 0.
- `python3 tests/state-test.py` → >= 6 passed, 0 failed.
- Grep gates: `def on_focus_change` present; `os.replace`, `except Exception`, `fullmatch` present; zero non-comment send_text/close/destroy/remote-control tokens.
- `kitty/state.conf` = `watcher state.py`.
</verification>

<success_criteria>
- The active-file writer exists, is unit-proven without kitty, and is structurally incapable of mutating windows.
- Nothing is deployed yet (no live-config edits); the kitty daily driver is untouched by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-running-state-seam/01-02-SUMMARY.md` when done.
</output>
