---
phase: 01-running-state-seam
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - bin/stail
  - tests/stail-test-2.sh
  - tests/stail-test-3.sh
  - tests/stail-test-4.sh
  - tests/stail-test-6.sh
  - CLAUDE.md
autonomous: true
requirements: [SEAM-01, SEAM-02]
must_haves:
  truths:
    - "stail list / list --json / active / active --json report correct state while NEVER consulting kdotool (proven by an invocation-logging stub staying empty)"
    - "cmd_switch still requires kdotool (_need_kdotool switch intact); its exchange-fallback decision now comes from live run markers, not the session-file grep"
    - "Dead markers are reaped on read; the exchange union reflects live board=exchange panes only (the strictly-more-accurate truth, OQ-2)"
    - "The --json byte-shapes and exit codes are unchanged: {lab,display,running} rows for list, {lab,display,exchange}/null + exit 0/1 for active"
    - "Full suite green at >= 147 baseline breadth plus the new state-seam assertions"
  artifacts:
    - path: "bin/stail"
      provides: "marker-scan _running_labs, state-sourced cmd_active, ungated cmd_list, marker-keyed cmd_trunk warning, _lab_in_exchange switch decision; _aggregate_labs/_lab_in_aggregate deleted; post-seam header"
      contains: "_lab_in_exchange"
    - path: "tests/stail-test-2.sh"
      provides: "B1-B5 rewritten to STATE fixtures encoding the new exchange truth; B6 raise stub kept"
    - path: "tests/stail-test-6.sh"
      provides: "reader-side sections: reap, hostile filename, no-kdotool-consult proof, active cross-check"
    - path: "CLAUDE.md"
      provides: "window-class contract point 5 rewritten to the state source; run/active added to the state-dir contract"
  key_links:
    - from: "$STATE/run/<lab>/<pid> markers"
      to: "cmd_list --json (frozen shape) -> Plasma widget"
      via: "_running_labs marker scan + _pane_alive + lazy reap"
      pattern: "_pane_alive"
    - from: "$STATE/active"
      to: "cmd_active --json (frozen shape) -> widget heading"
      via: "first-line read + charset gate + _running_labs cross-check"
      pattern: "STATE.*/active"
    - from: "cmd_switch"
      to: "kdotool windowactivate"
      via: "_win_ids raise handle (kept) + _lab_in_exchange marker decision (new)"
      pattern: "windowactivate"
---

<objective>
Flip detection to the stail-owned state source: rewrite `_running_labs` as a marker scan with lazy reap, re-source `cmd_active` from `$STATE/active` (with liveness cross-check), drop the kdotool gates from list/active, re-key `cmd_trunk`'s already-up warning and `cmd_switch`'s exchange-fallback decision to live markers, delete the dead session-file-grep helpers, and rewrite the tests that asserted the old mechanism (test-2 B1-B5, test-3 R2, test-4 #10) against the new source — preserving every behavioral assertion at >= baseline breadth. Adopts the live-marker exchange-union truth per open-question resolution OQ-2. Completes SEAM-01 and SEAM-02 in code; docs (stail header + CLAUDE.md contract point 5) flip in the same plan so the repo never describes a world it doesn't implement.

Purpose: `list`/`active` work with kdotool absent; kdotool survives only on the raise path.
Output: post-seam bin/stail; rewritten tests 2/3/4; extended test-6; updated CLAUDE.md; full suite green at >= 160 total passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-running-state-seam/01-RESEARCH.md
@.planning/phases/01-running-state-seam/01-PATTERNS.md
@bin/stail
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan produces: `_lab_in_exchange` (new bash function: $1=lab -> 0 iff the lab has a LIVE marker with `board=exchange`), the rewritten `_running_labs` (marker scan, no kdotool), the state-sourced `cmd_active`, the ungated `cmd_list`, the marker-keyed `cmd_trunk` warning, the marker-decided `cmd_switch` exchange fallback, the DELETION of `_aggregate_labs`/`_lab_in_aggregate`, reader-side test-6 sections, rewritten test-2/3/4 fixtures, and the post-seam docs (bin/stail header, CLAUDE.md contract point 5 + state-dir contract). See 01-01-PLAN.md for the full phase-wide symbol registry.

<tasks>

<task type="auto">
  <name>Task 1: Re-source list/active + rewrite test-2 B1-B5 + extend test-6 (reader side)</name>
  <files>bin/stail, tests/stail-test-2.sh, tests/stail-test-6.sh</files>
  <read_first>
    - bin/stail lines 132-155 (`_running_labs` + its comment block; `_need_kdotool`)
    - bin/stail lines 619-666 (`cmd_active` + `cmd_list` — the emission blocks that must stay byte-identical)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Code Examples: verified `_running_labs` replacement and `cmd_active` re-source drafts; Pitfall 6; "--json contract" section)
    - .planning/phases/01-running-state-seam/01-PATTERNS.md (cmd_active rewrite + cmd_list sections; test-2 rewrite notes)
    - tests/stail-test-2.sh (full — STUB_CLASSES/STUB_ACTIVE kdotool dispatch lines 39-47; B1-B5 lines 38-82; B6 lines 84-93)
    - tests/stail-test-6.sh (as created by plan 01-03 — append, don't restructure)
  </read_first>
  <action>
    bin/stail:
    - Replace `_running_labs` (body AND its preceding comment block, lines 132-148) with the marker-scan implementation per the verified RESEARCH.md draft: `shopt -s nullglob`; iterate `"$STATE"/run/*/`; per file, gate the filename with `[[ "$pid" =~ ^[0-9]+$ ]] || continue` (never trust a stray name near /proc or rm); read the `start=` line; live iff `_pane_alive "$pid" "$start"`, else `rm -f` (lazy reap, race-idempotent); a lab is up if ANY marker is live; emit one lab per line; emit `exchange` once if any live marker anywhere carries `board=exchange`. Output contract unchanged (one lab per line, `exchange` included when up) so callers keep their shape. New comment block documents the scan + reap + the NEW exchange truth: only labs with a live exchange pane count (live accuracy adopted per OQ-2 — a closed exchange line no longer reports its lab running; the old `_aggregate_labs` file-grep over-report is retired).
    - `cmd_list`: delete `_need_kdotool list` (line 646); update the line-650 comment from "one kdotool snapshot" to "one state-dir scan". The JSON/text emission loops stay byte-identical.
    - `cmd_active`: delete `_need_kdotool active` (line 623); replace the kdotool read (line 626) and the class case (lines 627-630) with: read the first line of `$STATE/active` when readable (`head -n1`); charset-gate `[[ "$lab" =~ ^[A-Za-z0-9._-]+$ ]] || lab=""` (never let junk reach JSON); crash-staleness cross-check — `_running_labs | grep -qxF -- "$lab" || lab=""` (a stale active file naming a dead board reports off-board); set `exchange=true` when `lab=exchange`. The emission block (lines 631-640) and the trailing `[ -n "$lab" ]` exit-code line stay byte-identical — this is the frozen widget contract.
    tests/stail-test-2.sh: rewrite B1-B5's fixtures from the kdotool stub (STUB_CLASSES/STUB_ACTIVE) to STATE fixtures: isolated `STATE=$(mktemp -d)/state` per part; live markers via `sleep 60 &` helpers with real /proc start times; `$STATE/active` written directly with the board under test. PRESERVE every behavioral assertion (list text shape, list --json validity via jsonok + running flags, active text/json shapes + exit codes, exchange union) but encode the NEW union truth: a lab counts as running-via-exchange ONLY when it has a live `board=exchange` marker; add the inverse case (lab whose exchange marker's helper PID was killed reads NOT running). Keep B6 (switch raise via the kdotool windowactivate stub + /tmp/stail-activate.log) untouched — raise stays kdotool.
    tests/stail-test-6.sh — append reader-side sections:
    §5 reap + hostile filename: seed a dead marker (real format, killed helper PID) and a hostile filename (`not-a-pid;x`) in an isolated STATE; one `_running_labs` call: dead marker file is REMOVED afterward, hostile file is ignored AND left untouched (never reaches /proc or rm), lab with only-dead markers absent from output.
    §6 no-kdotool-consult proof: install a logging `kdotool` stub function/PATH-shim that appends to a log and fails; run `cmd_list`, `cmd_list --json`, `cmd_active`, `cmd_active --json` against fixtures; assert correct output AND the log stays EMPTY (detection never consults kdotool — SEAM-01's literal test). Then assert the switch gate survives: `sed -n '/^cmd_switch()/,/^}/p'` on the sourced copy contains `_need_kdotool switch` (source assertion; behavioral raise coverage lives in test-3).
    §7 active cross-check: `$STATE/active` naming a lab with NO live marker -> `cmd_active` prints nothing, exits 1, and `--json` emits the null shape `{"lab":null,"display":null,"exchange":false}`; missing file -> same off-board behavior (Pitfall 5's degraded mode is correct, not wrong).
  </action>
  <verify>
    <automated>STAIL_BIN=$PWD/bin/stail bash tests/stail-test-2.sh && STAIL_BIN=$PWD/bin/stail bash tests/stail-test-6.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^_running_labs()/,/^}/p' bin/stail | grep -v '^\s*#' | grep -c kdotool` = 0; same gate = 0 for `cmd_list` and `cmd_active` function bodies.
    - test-6 §6's kdotool log is empty after all four list/active invocations (assertion inside the suite).
    - `cmd_active --json` fixture outputs byte-match `{"lab":"zlab","display":"Zlab","exchange":false}` for an on-board fixture and `{"lab":null,"display":null,"exchange":false}` for off-board; exit codes 0/1 respectively (asserted in test-2/test-6).
    - Dead-marker reap and hostile-filename non-handling asserted (test-6 §5 passes).
    - test-2 reports 0 failed with assertion count >= its pre-rewrite count; B6's activate-log raise assertions still pass unchanged.
  </acceptance_criteria>
  <done>list/active are state-sourced with frozen output shapes, proven to never consult kdotool; new exchange truth and staleness cross-check are regression-locked.</done>
</task>

<task type="auto">
  <name>Task 2: Re-key trunk warning + switch exchange decision; delete dead grep helpers; rewrite test-3 R2 + test-4 #10</name>
  <files>bin/stail, tests/stail-test-3.sh, tests/stail-test-4.sh</files>
  <read_first>
    - bin/stail lines 481-518 (`cmd_trunk` — already-up warning at 505-510), lines 668-700 (`cmd_switch` — `_need_kdotool` at 671, windowactivate at 681/691, exchange fallback at 687), lines 167-177 (`_aggregate_labs`/`_lab_in_aggregate` + banner — to delete)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Pattern 4: decision from state, raise via kdotool)
    - .planning/phases/01-running-state-seam/01-PATTERNS.md (cmd_switch + cmd_trunk + test-3/test-4 rewrite sections)
    - tests/stail-test-3.sh (full — R2 switch-decision stubs; the lines 31/34 direct invocations are STAIL_BIN-parametrized by plan 01-01)
    - tests/stail-test-4.sh (full — #10 already-up stub at lines 87-90)
  </read_first>
  <action>
    bin/stail:
    - Add `_lab_in_exchange()` in the run-marker shared section (next to `_running_labs`): $1=lab -> returns 0 iff `$STATE/run/$1/` contains a live marker (filename `^[0-9]+$` gate + `_pane_alive`) whose content has a `board=exchange` line. Same scan discipline as `_running_labs` (nullglob, reap-free read — reaping stays centralized in `_running_labs`).
    - `cmd_trunk` (lines 508-510): replace the `command -v kdotool >/dev/null 2>&1 && [ -n "$(_win_ids "$lab")" ]` condition with `_running_labs | grep -qxF -- "$lab"`; KEEP the warning text containing "already up" verbatim (test-4 greps it case-insensitively); update the preceding comment (505-507) to name run markers as the source.
    - `cmd_switch` (line 687): replace `_lab_in_aggregate "$lab"` with `_lab_in_exchange "$lab"`. KEEP `_need_kdotool switch` (line 671), both `kdotool windowactivate` calls (681, 691), the duplicate-windows warning, and the fall-through-to-launch tail (695-699) — the existing structure already degrades to launch when state says up but no window is found, which satisfies Pattern 4's fall-through requirement.
    - Delete `_aggregate_labs`, `_lab_in_aggregate`, and their banner comment (lines 167-177) — first grep-prove zero remaining callers; post-seam, anything still grepping the session file for detection is the Pitfall-6 warning sign.
    tests/stail-test-3.sh: rewrite the R2 switch-DECISION fixtures from kdotool-search stubs / session-file seeding to STATE marker fixtures (live `sleep` helpers): standalone-up case (kdotool stub still returns a window id for the raise — keep the activate-log raise stubs exactly as B6 models them); exchange-fallback case — lab live ONLY via a `board=exchange` marker -> switch raises the exchange window (activate log shows the exchange id); not-running case -> falls through to launch (launch stub fires). Preserve the raise-first-id and duplicate-warning assertions.
    tests/stail-test-4.sh #10: replace the `kdotool(){ case "$*" in "search --class ^switchtail-claude$") ...}` stub with a live run-marker fixture for the lab; keep the `grep -qi 'already up'` assertion verbatim.
  </action>
  <verify>
    <automated>STAIL_BIN=$PWD/bin/stail bash tests/stail-test-3.sh && STAIL_BIN=$PWD/bin/stail bash tests/stail-test-4.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' bin/stail | grep -cE '_aggregate_labs|_lab_in_aggregate'` = 0 (definitions and callers both gone).
    - `sed -n '/^cmd_switch()/,/^}/p' bin/stail | grep -c '_need_kdotool switch'` = 1 and `sed -n '/^cmd_switch()/,/^}/p' bin/stail | grep -c 'windowactivate'` = 2 (raise path intact).
    - `sed -n '/^cmd_trunk()/,/^}/p' bin/stail | grep -v '^\s*#' | grep -c kdotool` = 0; the already-up warning text still contains "already up".
    - `grep -c '^_lab_in_exchange()' bin/stail` = 1.
    - test-3 and test-4 report 0 failed with assertion counts >= their pre-rewrite counts.
  </acceptance_criteria>
  <done>Every detection decision reads markers; every activation still goes through kdotool; dead helpers removed; the R2/already-up behavior set is locked against the new source.</done>
</task>

<task type="auto">
  <name>Task 3: Post-seam docs (stail header + CLAUDE.md) + full-suite breadth gate</name>
  <files>bin/stail, CLAUDE.md</files>
  <read_first>
    - bin/stail lines 1-28 (header — the lines 19-23 paragraph asserting kdotool introspection is now false), lines 619-621 + 643-644 (subcommand section comments)
    - CLAUDE.md (full — the 6-point window-class contract, the hold-marker 2-way-contract paragraph, "What lives here", "Verification gate")
    - .planning/phases/01-running-state-seam/01-RESEARCH.md ("State of the Art" + the deprecated-docs note; Project Constraints item 1)
  </read_first>
  <action>
    bin/stail header (lines 19-23): rewrite the paragraph to the post-seam truth — window class stays `switchtail-<lab>` (the contract `.desktop` StartupWMClass and the raise path key on); running detection reads stail-owned markers `$STATE/run/<lab>/<pid>` (liveness via /proc + start-time) and the focused board comes from `$STATE/active` (written by the state.py focus watcher); kdotool is consulted ONLY by `switch` for raise/focus. Remove the now-false "active/list/switch introspect the live windows via kdotool" sentence. Touch up the `cmd_active` (619-621) and `cmd_list` (643-644) section comments to say state-read, not window-introspection.
    CLAUDE.md:
    - Window-class contract point 5: replace "`kdotool` greps that class for running detection (`_running_labs` in `bin/stail`)" with: running detection reads stail-owned markers `$STATE/run/<lab>/<pid>` written by `stail line` (liveness = /proc + start-time); kdotool greps the class ONLY on the raise path (`stail switch`).
    - Point 6 stays (widget keys off `stail list --json`) — the JSON shape is unchanged; the data behind it now derives from markers.
    - Extend the hold-marker 2-way-contract paragraph: the state dir now also carries `run/<lab>/<pid>` markers (written by `stail line`, read+reaped by list/active/switch) and `active` (written by `kitty/state.py`, read by `stail active`) — change the state-dir layout in `bin/stail`, `hold.py`, AND `state.py` together or not at all.
    - "What lives here": add the state watcher to the kitty/ surface enumeration (hold, swap, tail, state, keys).
    - "Verification gate": update the assertion-count reference from "147 assertions" to the actual post-phase total printed by the suite (read it from the breadth-gate run below), keeping the run-green-before-commit rule.
    Breadth gate: run `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh`; capture every per-suite `RESULT:` line; require ALL SUITES PASSED, 0 failed everywhere, and total passes >= 160 (147 baseline + state-seam + watcher assertions). Record the exact total in the SUMMARY (it becomes the new baseline number criterion 4 is judged against).
  </action>
  <verify>
    <automated>[ "$(grep -c 'introspect the live windows via kdotool' bin/stail)" -eq 0 ] && [ "$(grep -c 'greps that class for running detection' CLAUDE.md)" -eq 0 ] && grep -q 'run/<lab>' CLAUDE.md && STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'introspect the live windows via kdotool' bin/stail` = 0; the header names `$STATE/run/` and `$STATE/active` and restricts kdotool to switch.
    - CLAUDE.md contract point 5 names the marker path and the raise-only kdotool role; the state-dir contract paragraph names stail + hold.py + state.py as co-owners; the kitty/ enumeration includes the state watcher.
    - `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED; summed per-suite passes >= 160; every suite 0 failed.
    - The new total assertion count is recorded in CLAUDE.md's verification-gate line and in the plan SUMMARY.
  </acceptance_criteria>
  <done>Repo docs describe only the post-seam world; the full suite is green at >= baseline-plus-new breadth against the edited tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $STATE/run + $STATE/active filesystem -> readers | marker filenames/content and active-file content flow into /proc paths, rm, and widget-bound JSON |
| argv (Plasma widget) -> cmd_switch/cmd_trunk | lab names from outside _discover_labs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-R1 | Tampering | stray/hostile filename in run/ reaching `/proc/<name>` or `rm` | medium | mitigate | `^[0-9]+$` filename gate before any use in `_running_labs` and `_lab_in_exchange` (Tasks 1-2); test-6 §5 seeds a hostile name and asserts it is ignored and untouched |
| T-01-R2 | Tampering | JSON injection via corrupted `$STATE/active` content into the widget-parsed `--json` output | high | mitigate | charset gate `^[A-Za-z0-9._-]+$` before emission in `cmd_active` (Task 1); run-dir basenames are only membership-compared against `_discover_labs` output, never emitted |
| T-01-R3 | Spoofing | stale active file naming a dead board (kitty crash, no loss event) | medium | mitigate | `_running_labs` cross-check empties `lab` on mismatch (Task 1); asserted by test-6 §7 |
| T-01-R4 | Spoofing | PID reuse counting a dead board as running | medium | mitigate | `_pane_alive` start-time comparison on every read (consumes plan 01-03's guard); test-2 dead-helper cases |
| T-01-R5 | DoS | reap race between concurrent readers (widget poll + manual list) | low | accept | `rm -f` is idempotent (exit 0 on already-gone); no shared-writer paths by construction (RESEARCH Pitfall 7) |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | zero package-manager installs this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED, total passes >= 160, 0 failed in every suite.
- kdotool appears in bin/stail function bodies ONLY inside `_win_ids`, `_need_kdotool`, and `cmd_switch`: `grep -v '^\s*#' bin/stail | grep -n kdotool` matches nothing outside those.
- `--json` shapes for list/active byte-match the pre-seam contract (test-2 + test-6 assertions).
- Docs (bin/stail header, CLAUDE.md point 5) assert the post-seam mechanism only.
</verification>

<success_criteria>
- SEAM-01: list/active/--json report from stail-owned state — kdotool-consult log provably empty.
- SEAM-02: removing kdotool would degrade raising only (switch exits 3 via _need_kdotool); listing is kdotool-free.
- Roadmap criterion 4's automated half: suite green at >= baseline breadth plus state-seam assertions.
</success_criteria>

<output>
Create `.planning/phases/01-running-state-seam/01-04-SUMMARY.md` when done.
</output>
