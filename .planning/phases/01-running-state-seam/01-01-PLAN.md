---
phase: 01-running-state-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stail-test-1.sh
  - tests/stail-test-2.sh
  - tests/stail-test-3.sh
  - tests/stail-test-4.sh
  - tests/stail-test-5.sh
  - tests/run-all.sh
autonomous: true
requirements: [SEAM-01, SEAM-02]
must_haves:
  truths:
    - "Running any suite with STAIL_BIN=<path> tests THAT stail binary, not the deployed symlink"
    - "Default invocation (no STAIL_BIN) behaves exactly as today: tests $HOME/.local/bin/stail"
    - "The full 147-assertion baseline passes unchanged in both modes"
  artifacts:
    - path: "tests/stail-test-1.sh"
      provides: "STAIL_BIN-parametrized harness"
      contains: "STAIL_BIN"
    - path: "tests/stail-test-5.sh"
      provides: "STAIL_BIN-parametrized harness (incl. the direct line-90 invocation)"
      contains: "STAIL_BIN"
    - path: "tests/run-all.sh"
      provides: "orchestrator documenting/echoing the stail under test"
      contains: "STAIL_BIN"
  key_links:
    - from: "STAIL_BIN env var"
      to: "cp source + every direct stail invocation in tests/stail-test-{1..5}.sh"
      via: "shell parameter default ${STAIL_BIN:-$HOME/.local/bin/stail}"
      pattern: "STAIL_BIN"
---

<objective>
Parametrize the regression harness so the suite can honestly test an edited checkout instead of silently re-testing the deployed live symlink (RESEARCH.md Pitfall 1: the live-symlink x worktree trap). This is the Wave-0 prerequisite adopted per open-question resolution OQ-4 — without it, every later plan's "suite green" proves nothing about the phase's edits.

Purpose: honest verification for plans 01-03/01-04, and a permanent dev-vs-live switch for the suite.
Output: all five bash suites + run-all.sh honor `STAIL_BIN` (default `$HOME/.local/bin/stail`, preserving today's behavior byte-for-byte).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-running-state-seam/01-RESEARCH.md
@.planning/phases/01-running-state-seam/01-PATTERNS.md
</context>

## Artifacts this phase produces

Phase-wide symbol registry (this plan produces only the STAIL_BIN harness entries; the rest land in plans 01-02..01-05 — listed here so review passes can exclude new symbols from drift checks):

| Symbol | Kind | Introduced by |
|---|---|---|
| `STAIL_BIN` | test-harness env var (default `$HOME/.local/bin/stail`) | 01-01 |
| `_run_mark` | bash function in bin/stail (writes `$STATE/run/<lab>/<pid>`) | 01-03 |
| `_pane_alive` | bash function in bin/stail (PID + start-time liveness) | 01-03 |
| `_lab_in_exchange` | bash function in bin/stail (live board=exchange marker test) | 01-04 |
| `_running_labs` | rewritten (marker scan, no kdotool) | 01-04 |
| `cmd_active` | rewritten (reads `$STATE/active`, no kdotool) | 01-04 |
| `cmd_list` | modified (no `_need_kdotool` gate) | 01-04 |
| `cmd_trunk` | modified (already-up warning keyed off markers) | 01-04 |
| `cmd_switch` | modified (exchange decision from markers; raise stays kdotool) | 01-04 |
| `cmd_line` | modified (4th argv `board`, calls `_run_mark` pre-exec) | 01-03 |
| `_emit_session` | modified (emits `--var board=` + 4th argv) | 01-03 |
| `_aggregate_labs`, `_lab_in_aggregate` | DELETED (dead post-seam) | 01-04 |
| `kitty/state.py` (`on_focus_change`, `_active_path`, `_BOARD_RE`) | new kitty watcher | 01-02 |
| `kitty/state.conf` (`watcher state.py`) | new conf | 01-02 |
| `tests/stail-test-6.sh` | new state-seam suite | 01-03 (extended 01-04) |
| `tests/state-test.py` | new watcher unit suite | 01-02 |
| `$STATE/run/<lab>/<pid>` | new state path (marker files: `start=`, `board=`, `kind=`, `sid=`) | 01-03 |
| `$STATE/active` | new state path (focused board name) | 01-02 |
| `--var board=` / `stail line <lab> [dir] [kind] [board]` | session grammar extension | 01-03 |
| `~/.config/kitty/state.py`, `~/.config/kitty/state.conf`, `include state.conf` | deploy artifacts | 01-05 |

<tasks>

<task type="auto">
  <name>Task 1: Parametrize STAIL_BIN across the five bash suites</name>
  <files>tests/stail-test-1.sh, tests/stail-test-2.sh, tests/stail-test-3.sh, tests/stail-test-4.sh, tests/stail-test-5.sh</files>
  <read_first>
    - tests/stail-test-1.sh (full — cp at line 9, `stail()` wrapper at line 15, direct `command ~/.local/bin/stail generate` at line 16)
    - tests/stail-test-2.sh (cp at line 32)
    - tests/stail-test-3.sh (cp at line 8; direct invocations at lines 31 and 34)
    - tests/stail-test-4.sh (cp at line 12)
    - tests/stail-test-5.sh (cp at line 12; direct `sh -c "$HOME/.local/bin/stail $cmdstr"` at line 90)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Pitfall 1)
  </read_first>
  <action>
    In each of the five suites, add `STAIL_BIN="${STAIL_BIN:-$HOME/.local/bin/stail}"` once near the top (immediately after `set -uo pipefail`), then replace every hardcoded reference to the deployed stail with `"$STAIL_BIN"`:
    - test-1: line 9 cp source; line 15 `stail()` wrapper body; line 16 direct `generate` invocation. Update the header comment (line 3) that names `~/.local/bin/stail` to mention the STAIL_BIN override.
    - test-2: line 32 cp source.
    - test-3: line 8 cp source; lines 31 and 34 direct `switch`/`line` invocations.
    - test-4: line 12 cp source.
    - test-5: line 12 cp source; line 90 — replace `$HOME/.local/bin/stail` inside the `sh -c` string with `$STAIL_BIN` (the path is space-free on this box; keep the existing interpolation style so the PATH-prepend stub at `/tmp/t5bin` keeps working).
    Mechanical substitution ONLY — no assertion, fixture, or behavior changes. The parameter default preserves today's behavior exactly when STAIL_BIN is unset.
  </action>
  <verify>
    <automated>bash tests/stail-test-1.sh && bash tests/stail-test-2.sh && bash tests/stail-test-3.sh && bash tests/stail-test-4.sh && bash tests/stail-test-5.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'local/bin/stail' tests/stail-test-*.sh | grep -v 'STAIL_BIN' | grep -v '^\S*:[0-9]*:\s*#' | wc -l` outputs 0 (every non-comment hardcoded path is gone except inside the STAIL_BIN default expansion).
    - `grep -c 'STAIL_BIN=' tests/stail-test-1.sh` >= 1, and likewise for suites 2-5.
    - Each suite run with no STAIL_BIN set reports the same "RESULT: N passed, 0 failed" counts as before the edit.
    - `STAIL_BIN=/nonexistent bash tests/stail-test-2.sh; [ $? -ne 0 ]` — a bogus override makes the suite fail, proving the parameter is actually honored.
  </acceptance_criteria>
  <done>All five suites source their functions and invoke stail exclusively through `$STAIL_BIN`; default behavior is byte-identical to today; baseline counts unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: run-all.sh documents and surfaces the stail under test</name>
  <files>tests/run-all.sh</files>
  <read_first>
    - tests/run-all.sh (full — 13 lines; header comment lines 2-3, suite loop line 7)
  </read_first>
  <action>
    Update the header comment (lines 2-3) to document the `STAIL_BIN` override ("set STAIL_BIN=<path> to test a checkout instead of the deployed ~/.local/bin/stail"). After the `here=` line, add one diagnostic echo printing `stail under test: ${STAIL_BIN:-$HOME/.local/bin/stail}` so Pitfall-1 mistakes are visible in every suite log. Do NOT change the suite loop in this plan (test-6/state-test wiring belongs to plan 01-03).
  </action>
  <verify>
    <automated>bash tests/run-all.sh | grep -q 'stail under test' && bash tests/run-all.sh | grep -q 'ALL SUITES PASSED' && STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh | grep -q 'ALL SUITES PASSED'</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all.sh` (default mode) prints the "stail under test:" line naming `$HOME/.local/bin/stail` and ends with ALL SUITES PASSED at the 147-assertion baseline.
    - `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` prints the repo path and ends with ALL SUITES PASSED (repo bin/stail is currently identical to the deployed symlink target, so counts match).
    - The suite loop (line 7) is unchanged in this plan.
  </acceptance_criteria>
  <done>run-all.sh advertises and echoes STAIL_BIN; full suite green in both default and override modes at identical assertion counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STAIL_BIN env -> test harness | operator-supplied path is copied and sourced by the suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-H1 | Tampering | suites source whatever STAIL_BIN names | low | accept | single-user box; STAIL_BIN is the operator's own override, same trust level as editing the test itself |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | zero package-manager installs this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `bash tests/run-all.sh` → ALL SUITES PASSED, baseline assertion counts unchanged (147).
- `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED.
- `STAIL_BIN=/nonexistent bash tests/stail-test-2.sh` → non-zero exit.
- No remaining non-comment hardcoded `~/.local/bin/stail` outside the STAIL_BIN defaults.
</verification>

<success_criteria>
- The suite can be pointed at any checkout via STAIL_BIN; default mode is byte-identical to today.
- 147-assertion baseline green in both modes.
- This is purely additive harness work: zero changes to bin/stail, kittens, fixtures, or assertions.
</success_criteria>

<output>
Create `.planning/phases/01-running-state-seam/01-01-SUMMARY.md` when done.
</output>
