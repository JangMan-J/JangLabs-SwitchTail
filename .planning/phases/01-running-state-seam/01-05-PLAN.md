---
phase: 01-running-state-seam
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - ~/.config/kitty/state.py (new relative symlink)
  - ~/.config/kitty/state.conf (new relative symlink)
  - ~/.config/kitty/kitty.conf (include state.conf)
  - ~/.config/kitty/sessions/labs/* (regenerated by stail generate)
autonomous: false
requirements: [SEAM-01, SEAM-02]
must_haves:
  truths:
    - "The seam code is live at $HOME/.local/bin/stail (the symlink's target tree contains _run_mark) before any deploy step runs"
    - "state.py loads into newly launched kitty boards (symlinks + kitty.conf include in place)"
    - "Regenerated session files carry --var board= on every launch line"
    - "Full suite green against the LIVE deployed path (no STAIL_BIN override)"
    - "The Plasma widget shows correct running state and heading with ZERO widget changes"
  artifacts:
    - path: "~/.config/kitty/state.py"
      provides: "live watcher deployment (relative symlink into this repo, tail.py form)"
    - path: "~/.config/kitty/kitty.conf"
      provides: "include state.conf directive"
      contains: "include state.conf"
  key_links:
    - from: "phase branch merge -> versioning on the main checkout"
      to: "$HOME/.local/bin/stail behavior"
      via: "live relative symlink (commit/merge = instant deploy)"
      pattern: "_run_mark"
    - from: "stail generate"
      to: "~/.config/kitty/sessions/labs/*.kitty-session"
      via: "_emit_session board stamping"
      pattern: "--var board="
---

<objective>
Deploy the seam to the live daily driver and verify it against reality: wire the state watcher into the live kitty config (relative symlinks + include, per CLAUDE.md runtime placement rules), regenerate session files so every launch line carries the board stamp, run the full suite against the live deployed path, then hand the operator the one-time relaunch step (pre-seam boards have no markers and read DOWN until relaunched — RESEARCH Runtime State Inventory) and the live checks no script can do: focus semantics (A3), pane-exit reaping (A2), and the zero-change widget criterion.

Purpose: roadmap success criteria 3 and 4, and the live halves of SEAM-01/SEAM-02.
Output: live deployment + human-verified live behavior; phase complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-running-state-seam/01-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan produces the deploy artifacts: `~/.config/kitty/state.py` and `~/.config/kitty/state.conf` (relative symlinks into this repo), the `include state.conf` line in `~/.config/kitty/kitty.conf`, and regenerated `~/.config/kitty/sessions/labs/*.kitty-session` files carrying `--var board=`. No new repo symbols. See 01-01-PLAN.md for the full phase-wide symbol registry.

<tasks>

<task type="auto">
  <name>Task 1: Merge-gate, deploy wiring, regen, live suite</name>
  <files>~/.config/kitty/state.py, ~/.config/kitty/state.conf, ~/.config/kitty/kitty.conf, ~/.config/kitty/sessions/labs/*</files>
  <read_first>
    - CLAUDE.md (Runtime placement rules — bin/kitty/tests are RELATIVE symlinks; PATH discipline — absolute $HOME/.local/bin/stail in GUI contexts; Branch model — versioning is the live WIP branch)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Pitfall 5 deploy order; Pitfall 1 merge-equals-deploy; Runtime State Inventory)
    - Output of `ls -la ~/.config/kitty/tail.py ~/.config/kitty/tail.conf` (the exact relative-symlink form to mirror: `../../JangLabs/switchtail/kitty/<file>`)
    - ~/.config/kitty/kitty.conf lines 3200-3212 (the existing include block: hold.conf at 3203, keys.conf at 3206, tail.conf at 3209)
  </read_first>
  <action>
    Execute in this order (Pitfall 5: symlinks -> include -> regen -> relaunch; the relaunch is Task 2's human step):
    1. MERGE GATE: `grep -c '_run_mark' "$HOME/.local/bin/stail"` must be >= 1, proving the live symlink's target (this repo's main checkout at /home/jangmanj/JangLabs/switchtail, branch `versioning`) carries the phase code. If 0: integrate the phase branch into `versioning` on the main checkout first (per the branch model, commit/merge = instant deploy — the widget polls the live symlink within seconds, so merge only with plan 01-04's suite green), then re-run the gate. Do not proceed past a failing gate.
    2. SYMLINKS: create `~/.config/kitty/state.py -> ../../JangLabs/switchtail/kitty/state.py` and `~/.config/kitty/state.conf -> ../../JangLabs/switchtail/kitty/state.conf` — RELATIVE links exactly mirroring the verified tail.py/tail.conf form (`ln -s` with the relative target, created from inside `~/.config/kitty/`). Idempotent: skip if a correct link already exists.
    3. INCLUDE: add the single line `include state.conf` to `~/.config/kitty/kitty.conf` adjacent to the existing `include tail.conf` (line ~3209). Idempotent: grep before adding; never duplicate. This is the daily driver's live config — make exactly this one-line addition, nothing else.
    4. REGEN: run `$HOME/.local/bin/stail generate` (absolute path per PATH discipline). Verify every regenerated `*.kitty-session` under `~/.config/kitty/sessions/labs/` now carries `--var board=` on each launch line (`grep -L -- '--var board=' ...` returns nothing).
    5. LIVE SUITE: from the main checkout, run `bash tests/run-all.sh` WITHOUT STAIL_BIN (defaults to the live deployed path) — must print ALL SUITES PASSED at the plan-01-04 breadth (>= 160 total passes, 0 failures anywhere).
  </action>
  <verify>
    <automated>grep -q '_run_mark' "$HOME/.local/bin/stail" && [ "$(readlink ~/.config/kitty/state.py)" = "../../JangLabs/switchtail/kitty/state.py" ] && [ "$(readlink ~/.config/kitty/state.conf)" = "../../JangLabs/switchtail/kitty/state.conf" ] && [ "$(grep -c '^include state.conf' ~/.config/kitty/kitty.conf)" -eq 1 ] && [ -z "$(grep -L -- '--var board=' ~/.config/kitty/sessions/labs/*.kitty-session)" ] && bash tests/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '_run_mark' "$HOME/.local/bin/stail"` succeeds (seam code live).
    - `readlink ~/.config/kitty/state.py` outputs exactly `../../JangLabs/switchtail/kitty/state.py`; same form for state.conf.
    - `grep -c '^include state.conf' ~/.config/kitty/kitty.conf` = 1 (present, not duplicated).
    - Zero session files lack the board stamp: `grep -L -- '--var board=' ~/.config/kitty/sessions/labs/*.kitty-session` outputs nothing.
    - `bash tests/run-all.sh` (no STAIL_BIN) → ALL SUITES PASSED, total passes >= 160, every suite 0 failed.
  </acceptance_criteria>
  <done>Watcher wired into the live kitty config, session files board-stamped, suite green against the deployed path. Nothing visible breaks for currently-running boards (they keep working; they just read DOWN until relaunched).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — relaunch, focus semantics, pane-exit reap, widget zero-change</name>
  <read_first>
    - .planning/phases/01-running-state-seam/01-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Assumptions A2/A3 — the two live-behavior assumptions this checkpoint discharges)
  </read_first>
  <files>none (live verification only — no file edits)</files>
  <action>Operator-driven live verification of the deployed seam: one-time board relaunch, then the seven observation steps in how-to-verify. No automation can stand in for Plasma rendering, KWin focus events, or a real pane exit (VALIDATION.md Manual-Only Verifications).</action>
  <what-built>
    The running-state seam is fully deployed: `stail list`/`active` now read stail-owned state (`$STATE/run/` markers + `$STATE/active`); the state.py focus watcher loads into every NEWLY launched kitty board; session files are regenerated with board stamps; kdotool survives only inside `stail switch`. Pre-seam boards (launched before this deploy) never wrote markers and never loaded state.py — they will read DOWN and won't feed `active` until relaunched. That one-time relaunch is the first step below; no code can backfill their PIDs reliably (RESEARCH Runtime State Inventory).
  </what-built>
  <how-to-verify>
    1. Relaunch: close any pre-seam SwitchTail boards (Ctrl+Shift+R per claude pane first if you want those sessions to resume via hold), then launch at least one lab board (launcher entry or `stail switch <lab>`) — and optionally the exchange board.
    2. List truth: run `stail list` and `stail list --json` — the relaunched board(s) show running, everything closed shows `-`/false; output matches reality exactly.
    3. Active gain (A3 half 1): focus the relaunched board, run `stail active --json` — it names that lab with exit 0.
    4. Active loss (A3 half 2): focus a NON-kitty window (e.g. browser), run `stail active` — empty output, exit 1; `stail active --json` prints the null shape.
    5. Pane-exit reap (A2): in the relaunched board, exit a pane (`/exit` in a claude line, or close a shell pane). If it was the board's last pane the window closes; run `stail list` — that lab now reads down (marker reaped on read).
    6. Widget zero-change (criterion 3): the Plasma panel heading tracks the focused board; the popup's running flags match `stail list` — with NO widget edits this phase. Run `journalctl --user -u plasma-plasmashell -n 50` — no errors naming the applet.
    7. Raise path intact (SEAM-02): `stail switch <lab>` on the running lab raises its window via kdotool as before.
  </how-to-verify>
  <acceptance_criteria>
    - Steps 2-7 all observed as described: list/active/widget agree with reality, active clears on focus loss to a non-kitty app, a dead pane stops counting, plasmashell journal clean, switch still raises.
    - Any mismatch between list/active/widget and reality, or any pane dying at boot, is a blocker — report it instead of approving.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — intrinsically manual (Plasma rendering, KWin focus, live pane exit); automated halves already covered by Task 1's live-path suite run</automated>
    <human-check>Steps 1-7 in how-to-verify, observed on the live desktop</human-check>
  </verify>
  <done>Operator typed "approved": list/active/widget agree with reality, active clears on non-kitty focus, dead panes stop counting, plasmashell journal clean, switch still raises — discharging assumptions A2/A3 and roadmap criterion 3.</done>
  <resume-signal>Type "approved" if all checks hold, or describe exactly which step failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> live config | symlinks + one include line wire repo code into the daily driver's kitty |
| deploy timing | the widget polls the live stail symlink every few seconds during deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-D1 | DoS | broken intermediate state on the live symlink while the widget polls | high | mitigate | merge gate: deploy steps run only after plan 01-04's suite is green and the merge is complete (Task 1 step 1); the kitty.conf edit is a single idempotent include line |
| T-01-D2 | DoS | a faulty watcher loaded into every new kitty process | high | mitigate | state.py ships with a blanket exception guard and a filesystem-only contract, unit-proven in plan 01-02 before this deploy; checkpoint watches for boot-time pane deaths (Task 2 acceptance) |
| T-01-D3 | Tampering | wrong/absolute symlink form breaking on clone/rename | low | mitigate | acceptance asserts the exact RELATIVE readlink target mirroring tail.py's form |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | zero package-manager installs this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Automated: merge gate, readlink targets, single include line, board-stamped session files, live-path suite green at >= 160.
- Human: relaunch + A2/A3 live semantics + widget zero-change + clean plasmashell journal + switch raise.
</verification>

<success_criteria>
- Roadmap criterion 1-2: live list/active run from stail-owned state; kdotool raise-only (verified live).
- Roadmap criterion 3: widget correct with zero widget changes.
- Roadmap criterion 4: full suite green on the live deployed path at >= baseline-plus-new breadth.
</success_criteria>

<output>
Create `.planning/phases/01-running-state-seam/01-05-SUMMARY.md` when done.
</output>
