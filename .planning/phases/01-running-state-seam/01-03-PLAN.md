---
phase: 01-running-state-seam
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - bin/stail
  - tests/stail-test-6.sh
  - tests/run-all.sh
autonomous: true
requirements: [SEAM-01]
must_haves:
  truths:
    - "Every pane booted via stail line (claude, shell, AND cmd: kinds) leaves a marker $STATE/run/<lab>/<pid> before its exec"
    - "A marker-write failure NEVER prevents the pane's exec — stail line still boots the pane with an unwritable state dir"
    - "Marker liveness is decidable later: /proc/<pid> exists AND its start time matches the recorded start= ⇒ alive"
    - "Every launch line emitted by _emit_session carries --var board= and passes the board as stail line's 4th argv"
  artifacts:
    - path: "bin/stail"
      provides: "_run_mark + _pane_alive helpers, run-marker contract banner, board stamping in _emit_session, marker write in cmd_line"
      contains: "_run_mark"
    - path: "tests/stail-test-6.sh"
      provides: "writer-side state-seam assertions (marker write, liveness, never-fail, e2e lifecycle)"
      min_lines: 60
    - path: "tests/run-all.sh"
      provides: "suite loop including stail-test-6.sh and state-test.py"
      contains: "stail-test-6.sh"
  key_links:
    - from: "bin/stail _emit_session ($cls)"
      to: "cmd_line 4th argv"
      via: "board=${cls#switchtail-} -> '--var board=' + trailing argv on every launch line"
      pattern: "--var board="
    - from: "bin/stail cmd_line"
      to: "$STATE/run/<lab>/<pid>"
      via: "_run_mark called after the cd guard, before kind dispatch; tmp + mv -f"
      pattern: "_run_mark"
---

<objective>
Build the write side of the running-state seam in `bin/stail`: PID-keyed run markers with a start-time PID-reuse guard (`_run_mark`), the liveness predicate (`_pane_alive`), board identity stamped once by the single emitter (`--var board=` + 4th argv), and the marker write in `cmd_line` for EVERY pane kind (per open-question resolution OQ-3: shell/cmd panes count toward "board running", parity with today's class-based counting). Plus the new test suite `tests/stail-test-6.sh` (writer-side sections) wired into run-all.sh.

This plan must not change any reader: `_running_labs`, `cmd_list`, `cmd_active`, `cmd_trunk`, `cmd_switch` stay untouched (plan 01-04 re-sources them), so the live system and the existing 147 assertions are unaffected — markers are written but not yet consumed.

Purpose: the mux-agnostic state source the whole phase (and the Zellij migration) rests on.
Output: bin/stail with writers + helpers; tests/stail-test-6.sh; run-all.sh running 8 suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-running-state-seam/01-RESEARCH.md
@.planning/phases/01-running-state-seam/01-PATTERNS.md
@bin/stail
@tests/stail-test-2.sh
@tests/stail-test-1.sh
</context>

## Artifacts this phase produces

This plan produces: bash functions `_run_mark` (args: lab, board, kind, sid-may-be-empty; returns 0 unconditionally) and `_pane_alive` (args: pid, recorded-start), the state path `$STATE/run/<lab>/<pid>` (content lines `start=`, `board=`, `kind=`, `sid=`), the session-grammar extension `--var board=` / `stail line <lab> [dir] [kind] [board]`, `tests/stail-test-6.sh`, and the run-all.sh wiring for test-6 + state-test.py. See 01-01-PLAN.md for the full phase-wide symbol registry.

<tasks>

<task type="auto">
  <name>Task 1: _run_mark + _pane_alive helpers with the run-marker contract banner</name>
  <files>bin/stail</files>
  <read_first>
    - bin/stail lines 220-247 (the hold-marker banner + `_hold_claim` — the house analog for atomic markers, charset gates, and contract-comment style)
    - bin/stail lines 28-35 (`set -uo pipefail` is live; `$STATE` derivation from XDG_STATE_HOME)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Code Examples: verified `_run_mark` and `_pane_alive` drafts; Pitfalls 2, 3, 7)
    - .planning/phases/01-running-state-seam/01-PATTERNS.md (run-marker helpers section)
  </read_first>
  <action>
    Insert a new shared section in bin/stail directly after `_hold_claim` (after line 247): a banner `# ---------- shared: per-pane RUN markers (running-state seam) ----------` followed by a contract paragraph in the hold-banner's prose style documenting the new 2-way contract: one marker file `$STATE/run/<lab>/<pid>` per live pane, written by `stail line` pre-exec (exec preserves the PID, so the filename IS the live agent process); content lines `start=<jiffies>` (overall field 22 of /proc/<pid>/stat), `board=<board>`, `kind=<kind>`, `sid=<sid-or-empty>` (sid informational, may be empty); readers treat a marker live iff /proc/<pid>/stat exists AND its start time matches, else `rm -f` it (lazy reap, race-idempotent); single-writer-per-file by PID-named construction (no locking — Pitfall 7); EVERY write step tolerates failure because a failed marker must degrade listing, never kill the pane (`set -uo pipefail` is live — Pitfall 2).
    Implement `_run_mark` ($1=lab $2=board $3=kind $4=sid) and `_pane_alive` ($1=pid $2=recorded-start) exactly per the verified RESEARCH.md Code Examples drafts: `mkdir -p` guarded `|| return 0`; stat read guarded `|| return 0`; comm-safe start-time parse — strip through the last `") "` first (`rest="${stat##*) }"`) then take awk field 20 of the remainder, NEVER `awk '{print $22}'` on the raw line (comm may contain spaces/parens — Pitfall 3); write via `> "$d/$$.tmp"` then `mv -f "$d/$$.tmp" "$d/$$"` (atomic, the `_hold_claim` rename primitive); final unconditional `return 0`. `_pane_alive` returns 0 iff `/proc/$1/stat` is readable and its comm-stripped field 20 equals `$2` — the identical parse on both sides.
  </action>
  <verify>
    <automated>bash -n bin/stail && [ "$(grep -c '^_run_mark()' bin/stail)" -eq 1 ] && [ "$(grep -c '^_pane_alive()' bin/stail)" -eq 1 ] && [ "$(grep -v '^\s*#' bin/stail | grep -c 'print \$22')" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/stail` exits 0.
    - `grep -c '^_run_mark()' bin/stail` = 1 and `grep -c '^_pane_alive()' bin/stail` = 1.
    - `sed -n '/^_run_mark()/,/^}/p' bin/stail | grep -c 'return 0'` >= 3 (mkdir guard, stat-read guard, and final unconditional return — the never-fail discipline is visible in source).
    - `grep -v '^\s*#' bin/stail | grep -c 'print \$22'` = 0 (only the comm-strip-then-field-20 parse exists).
    - Both sides parse the stat line comm-safely: `grep -c '##\*) ' bin/stail` >= 2 (write side + read side).
    - No reader function (`_running_labs`, `cmd_list`, `cmd_active`, `cmd_trunk`, `cmd_switch`) is modified: `git diff bin/stail` touches only the new section.
  </acceptance_criteria>
  <done>Helpers exist in house style with the documented contract banner; never-fail and comm-safe-parse disciplines are source-assertable; no reader changed.</done>
</task>

<task type="auto">
  <name>Task 2: Board stamping in _emit_session + marker write in cmd_line</name>
  <files>bin/stail</files>
  <read_first>
    - bin/stail lines 249-294 (`_emit_session` — single emitter; flags built additively at 281-283; launch line at 284)
    - bin/stail lines 410-479 (`cmd_line` — optional-arg defaults at 431-432, `mkdir -p "$STATE"` + cd guard at 433-434, kind dispatch case at 435)
    - bin/stail lines 37-58 (usage() heredoc) and lines 9-16 (header subcommand list)
    - .planning/phases/01-running-state-seam/01-RESEARCH.md (Pattern 2; "Board stamping in the single emitter" example)
    - .planning/phases/01-running-state-seam/01-PATTERNS.md (_emit_session + cmd_line sections)
  </read_first>
  <action>
    `_emit_session`: derive `board="${cls#switchtail-}"` once alongside the existing locals (lines 265-266); extend the launch line (line 284) with `--var board=$board` placed with the other `--var` tags, and append `$board` as the 4th argv to `stail line` (after the quoted `"$kind"`). One edit point — every session shape (per-lab file, exchange, transient trunk, transient patch) inherits it through the single emitter.
    `cmd_line`: add `board` to the local list; default `board="${4:-$lab}"` next to the existing `dir=`/`kind=` defaults (lines 431-432) so pre-regen 3-arg session files keep working; defensively validate `[[ "$board" =~ ^[A-Za-z0-9._-]+$ ]] || board="$lab"` (degrade, never abort — board reaches marker file content that `_running_labs` will parse; mirrors the `_require_valid_lab` charset, per V5 input-validation). Call `_run_mark "$lab" "$board" "$kind" ""` at ONE call site: immediately after the cd guard succeeds (line 434) and before the `case "$kind"` dispatch — so claude, shell, and cmd: panes ALL write markers (OQ-3 decision: parity with class-based counting). Discretionary decision, document in a code comment: the marker is written pre-dispatch with `sid=` empty rather than per-branch after sid resolution — minimizes new code between the cd guard and exec (Pitfall 2) and no Phase-1 reader consumes sid; the Task-1 contract banner already declares sid optional.
    Update the three usage surfaces to advertise the optional arg: header comment line 11, usage() heredoc line 42, and the `${1:?usage: ...}` message at line 429 — all become `stail line <lab> [dir] [kind] [board]`.
    Compatibility note (no code, record in summary): old 3-arg session files default board to lab; new 4-arg files are harmless to the pre-seam deployed stail (a 4th argv is simply ignored), so regen order at deploy is not load-bearing.
  </action>
  <verify>
    <automated>bash -n bin/stail && tmp=$(mktemp -d) && sed '/^# ---------- dispatch ----------/,$d' bin/stail > "$tmp/fns" && bash -c "source '$tmp/fns'; E_DIR=(/tmp); E_LAB=(zlab); E_KIND=(claude); E_TITLE=(Z); _emit_session switchtail-zlab tall 0" | grep -qE -- '--var board=zlab' && XDG_STATE_HOME="$tmp/xs" bash bin/stail line zlab /tmp 'cmd:true' zboard && grep -q '^board=zboard$' "$tmp/xs/switchtail/run/zlab/"* && STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `_emit_session` fixture output (one claude pane, class switchtail-zlab) contains `--var board=zlab` on the launch line AND the line ends with `zlab` as stail line's 4th argv.
    - `XDG_STATE_HOME=<tmpdir> bash bin/stail line zlab /tmp 'cmd:true' zboard` exits 0 (cmd:true execs and exits cleanly) and leaves exactly one file under `<tmpdir>/switchtail/run/zlab/` whose content has a numeric `start=` line plus `board=zboard`, `kind=cmd:true`, `sid=` lines.
    - `XDG_STATE_HOME=<read-only-dir> bash bin/stail line zlab /tmp 'cmd:true'` STILL exits 0 — the marker failure degrades silently, the pane boots (Pitfall 2 e2e).
    - The three usage surfaces (header line 11, usage() heredoc, cmd_line `${1:?usage}` message) all read `stail line <lab> [dir] [kind] [board]`.
    - `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED (existing 147 assertions untouched by the writer-only changes).
  </acceptance_criteria>
  <done>Board identity flows emitter -> argv -> marker; markers written for all pane kinds pre-exec; never-fail proven e2e; baseline suite green against the edited tree.</done>
</task>

<task type="auto">
  <name>Task 3: tests/stail-test-6.sh (writer-side sections) + run-all.sh wiring</name>
  <files>tests/stail-test-6.sh, tests/run-all.sh</files>
  <read_first>
    - tests/stail-test-2.sh (harness boilerplate: ok/no counters, jsonok, cp + dispatch-strip + source pattern at lines 1-9 and 32-35)
    - tests/stail-test-1.sh (§6 isolated-STATE fixture pattern, lines 67-77 — including the hostile-filename seed)
    - tests/run-all.sh (suite loop line 7; python suites at lines 9-10)
    - .planning/phases/01-running-state-seam/01-VALIDATION.md (fixture rules: live markers via background `sleep` helper PIDs with real /proc start times; no fixed sleeps)
  </read_first>
  <action>
    Create `tests/stail-test-6.sh` with the house harness: `set -uo pipefail`; ok/no counters; `STAIL_BIN="${STAIL_BIN:-$HOME/.local/bin/stail}"`; copy `"$STAIL_BIN"` to a UNIQUE temp path (`/tmp/stail-fns6.sh`, not the shared /tmp/stail-fns.sh, to avoid cross-suite clobber), strip from `^# ---------- dispatch ----------`, source it; `RESULT: N passed, M failed` summary tail with `[ "$fail" -eq 0 ]` exit. Writer-side sections (reader-side sections are appended by plan 01-04):
    §1 marker write: isolated `STATE=$(mktemp -d)/state`; call `_run_mark zlab zboard claude ""` in the suite's own shell; assert `$STATE/run/zlab/$$` exists; its `start=` value equals the suite's own /proc/$$/stat comm-stripped field 20; `board=zboard` and `kind=claude` lines present; no `*.tmp` leftover in the dir.
    §2 liveness: start `sleep 60 &` helper; read its REAL start time from /proc/<pid>/stat (same comm-strip parse); assert `_pane_alive <pid> <start>` returns 0; `_pane_alive <pid> <start+1>` returns 1 (start-time mismatch = PID reuse = dead); kill the helper and `wait`; assert `_pane_alive <pid> <start>` returns 1.
    §3 never-fail: with `STATE` pointed somewhere unwritable (e.g. a `chmod 555` tempdir subpath), `_run_mark zlab zlab claude ""` returns 0. E2E: `XDG_STATE_HOME=<ro-dir> bash "$STAIL_BIN" line zlab /tmp 'cmd:true'` exits 0.
    §4 e2e lifecycle: writable `XDG_STATE_HOME` tempdir; run `bash "$STAIL_BIN" line zlab /tmp 'cmd:true' zboard`; assert exactly one marker exists under `.../switchtail/run/zlab/` with `board=zboard`; assert its named PID is dead (the exec'd `true` exited), i.e. `_pane_alive` on its filename+start returns 1 — the marker is reap-eligible for plan 01-04's reader.
    `tests/run-all.sh`: add `stail-test-6.sh` to the bash loop (line 7) and add a `python3 "$here/state-test.py"` invocation in the same style as the existing python suites (lines 9-10). Plan 01-02 created state-test.py; this plan owns the wiring because run-all.sh is single-owner per wave.
  </action>
  <verify>
    <automated>STAIL_BIN=$PWD/bin/stail bash tests/stail-test-6.sh && STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `STAIL_BIN=$PWD/bin/stail bash tests/stail-test-6.sh` reports >= 10 passed, 0 failed.
    - test-6 uses `/tmp/stail-fns6.sh` (unique), an isolated STATE tempdir, and live `sleep` helper PIDs with real /proc start times — `grep -c 'sleep 60' tests/stail-test-6.sh` >= 1 and no fixed-duration sleeps used as synchronization.
    - `grep -n 'stail-test-6.sh' tests/run-all.sh` and `grep -n 'state-test.py' tests/run-all.sh` both match within the suite-execution lines.
    - `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED with total reported passes >= 157 (147 baseline + >= 10 new) and every suite reporting 0 failed.
  </acceptance_criteria>
  <done>Writer-side state-seam behavior is regression-locked; the suite now runs 8 suites and is green against the edited tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv (widget/session files) -> cmd_line | lab/board/kind arrive from launch lines and the Plasma widget |
| $STATE/run filesystem -> future readers | marker filenames/content consumed by plan 01-04's readers |
| /proc -> liveness | kernel-owned ground truth for PID + start time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-M1 | Tampering | path traversal via `lab`/`board` argv reaching `$STATE/run/<lab>/` | medium | mitigate | `_require_valid_lab` already gates `lab` (line 430); `board` re-validated `^[A-Za-z0-9._-]+$` with degrade-to-lab fallback (Task 2); board is written as file CONTENT only, never a path component |
| T-01-M2 | Spoofing | PID reuse making a dead pane read alive | medium | mitigate | `start=` jiffies recorded at write, compared at read (`_pane_alive`); comm-safe parse on both sides (Task 1); asserted by test-6 §2 |
| T-01-M3 | DoS (self-inflicted) | marker write failure aborting cmd_line pre-exec under `set -uo pipefail`, killing an agent pane on the daily driver | high | mitigate | `_run_mark` returns 0 unconditionally with `|| return 0` on every fallible step (Task 1); e2e unwritable-STATE boot test (test-6 §3) |
| T-01-M4 | Tampering | half-written marker read by a concurrent reader | low | mitigate | tmp + `mv -f` atomic rename (same-FS); single-writer-per-file by PID-named construction |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | zero package-manager installs this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `STAIL_BIN=$PWD/bin/stail bash tests/run-all.sh` → ALL SUITES PASSED, total passes >= 157, 0 failures anywhere.
- E2E: `stail line` writes a complete marker for cmd: panes and boots the pane even with an unwritable state dir.
- `git diff bin/stail` shows NO changes inside `_running_labs`, `cmd_list`, `cmd_active`, `cmd_trunk`, `cmd_switch` (readers are plan 01-04's exclusive territory).
</verification>

<success_criteria>
- Run markers are written for every pane kind, atomically, with the PID-reuse guard, and can never kill a pane.
- Board identity is stamped once at the emitter and transported by argv + user-var.
- Detection still runs on the old kdotool path — the live system's behavior is unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-running-state-seam/01-03-SUMMARY.md` when done.
</output>
