---
context: default
phase: pre-phase (bootstrap complete, Phase 1 not yet planned)
task: null
total_tasks: null
status: paused
last_updated: 2026-06-12T07:11:25.875Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Baking Zellij API details from training data | The roadmap deliberately freezes NO Zellij plugin API names/signatures — training-data knowledge of the zellij-tile API is likely stale | advisory | workflow.research is enabled; plan-phase for Phase 2+ must pull live Zellij plugin docs (Context7 /find-docs or upstream) before naming any API |

_No blocking constraints were discovered this session (no failures occurred); the Blocking Constraints section is intentionally absent._

<current_state>
SwitchTail just completed its GSD project bootstrap on branch `versioning` (working
tree clean at `175b0a0`). Nothing has been planned or executed beyond the planning
artifacts themselves — we are positioned immediately BEFORE `/gsd-plan-phase 1`.

The project pivoted hard this session: the milestone is no longer "harden the kitty
system" but **rebuild SwitchTail as a Zellij WASM plugin, retire kitty AND the
Plasma 6 widget, success = functional parity**. Both pivot decisions are LOCKED
owner decisions in PROJECT.md (2026-06-11), explicitly superseding the ingested
direction report's defer-the-mux-swap verdict.
</current_state>

<completed_work>

Completed this session (all committed):
- GSD configured via /gsd-config — adaptive model profile, per-phase branching
  (gsd/phase-{phase}-{slug}), intel + graphify enabled, code_review / ui_review /
  context_warnings disabled; saved as global defaults (~/.gsd/defaults.json)
- /gsd-ingest-docs (mode=new): docs/direction-report-2026-06.md classified DOC and
  synthesized into .planning/intel/ (0 conflicts); session handoff excluded as a
  pattern false-positive — commit c9345e6
- Roadmap generated by gsd-roadmapper: 6 phases, 24 requirements, all mapped
- Pivot widened (widget retirement) and applied consistently across PROJECT.md /
  REQUIREMENTS.md / ROADMAP.md / STATE.md — commit 175b0a0
  (CUT-01 reworded to plasmoid retirement; DECK-03 in-plugin launcher/introspector
  added; PLUG-02 widened with the no-widget desktop entry-point question; UI hint
  moved Phase 6 → Phase 5)
</completed_work>

<remaining_work>

- Everything. No phase has been planned or executed:
  - Phase 1 Running-State Seam (on the CURRENT kitty system — stail owns running
    state; kdotool shrinks to raise/focus)
  - Phase 2 Zellij Plugin Feasibility PoC (four fused kitty roles + launcher/
    introspector role + no-widget desktop entry-point story; assess prior art)
  - Phase 3 Core Lifecycle on Zellij · Phase 4 Hold/Resume on Zellij ·
    Phase 5 Interaction Layer (UI surface) · Phase 6 Parity Cutover (retire kitty
    + plasmoid)
</remaining_work>

<decisions_made>

- LOCKED (owner, 2026-06-11): replace kitty foundation with Zellij; SwitchTail
  becomes a large Zellij WASM plugin (Rust/zellij-tile presumed — Phase 2 research
  confirms). Fires the report's "mux migration is scheduled" trigger.
- LOCKED (owner, 2026-06-11): retire the Plasma 6 widget too; the plugin contains
  the whole system surface. Widget stays installed through Phase 5 as a live
  verification probe for the state seam; uninstalled in Phase 6.
- `--json` contract downgraded: stable CLI/scripting/systemd contract, no longer a
  frozen GUI integration boundary.
- Parity metric: current verified behavior set on Zellij, suite green at
  ≥147-assertion breadth; widget functionality satisfied by in-plugin equivalents.
- Kitty v1 stays the daily driver until Phase 6 cutover — phases 1–5 carry explicit
  do-not-break-the-driver criteria.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)

1. `switchtail/CLAUDE.md` — lab conventions: window-class contract, runtime
   placement, PATH discipline, verification gate, branch model (WIP on
   `versioning`, merge phase branches there — NOT to `main`)
2. `.planning/PROJECT.md` — the locked pivot decisions and parity definition
3. `.planning/ROADMAP.md` — the 6-phase arc and per-phase success criteria
4. `.planning/intel/SYNTHESIS.md` then `.planning/intel/context.md` — the ingested
   direction report's coupling analysis (four fused kitty roles, friction matrix,
   migration playbook) that shaped phase ordering
5. `docs/direction-report-2026-06.md` — full source if intel summaries are not
   enough (note: its defer-verdict is superseded; its migration prescriptions hold)

## Infrastructure State

- kitty-based SwitchTail v1: live, daily-dogfooded, 147-assertion suite green —
  MUST stay working through phases 1–5
- Plasma widget: installed at ~/.local/share/plasma/plasmoids/ — stays until Phase 6
- Branch: `versioning` at 175b0a0, clean; per-phase branches configured
  (gsd/phase-{phase}-{slug})
- intel.enabled and graphify.enabled are on in config but no index/graph has been
  built yet (/gsd-map-codebase, /gsd-graphify never run)
- gsd-tools emits a harmless warning about unknown config keys (tavily_search,
  ref_search, perplexity, jina) in .planning/config.json
- No background processes running

<context>
The whole session was planning-only; no source files changed. The mental model:
the ingested direction report did the deep coupling analysis (kitty = four fused
roles; interaction layer ~28% of source is a per-backend paradigm rewrite; the
state seam must land BEFORE the migration, emitters already unified in ee250e1),
and the owner then deliberately fired the migration trigger the report had defined.
The roadmap encodes the report's own playbook for exactly this scenario. Zellij
permissions should make the "watcher can't kill a pane" property structural —
that's WATCH-02, a deliberate win over kitty. Prior art (zellij-attention,
zellaude, sessionizer family) is to be assessed in Phase 2, not blindly rebuilt.
</context>

<next_action>
Start with: /gsd-plan-phase 1 — plans the Running-State Seam on the current kitty
system; will create branch gsd/phase-1-running-state-seam. Merge it back into
`versioning` when done, not `main`.
</next_action>
