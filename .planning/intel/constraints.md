# Constraints (from SPECs)

_Synthesized 2026-06-11 from .planning/intel/classifications/ (mode: new)._

No SPECs were present in the ingest set. No api-contract / schema / nfr / protocol
constraints extracted.

Note: docs/direction-report-2026-06.md (DOC) describes de-facto contracts in the
existing codebase (the `--json` contract every external surface binds to; the
park/resume marker protocol's cwd-scoped-continue invariant; the window-class
contract). These are observed properties of the implementation, not normative SPEC
documents; they are recorded in `context.md` with source attribution.
